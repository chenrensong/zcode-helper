# 安全策略

ZCode Helper 处理的是你的 ZCode 账号凭据，安全问题是最高优先级。

## 报告漏洞

如果你发现了安全漏洞，**请不要公开提交 Issue**。请通过 GitHub Security Advisories
（仓库 Security 标签页 → Report a vulnerability）私密报告，或联系仓库维护者。

我们会在收到报告后尽快回应。

## 支持版本

| 版本 | 支持状态 |
| --- | --- |
| 1.0.x | ✅ |
| < 1.0 | ❌ |

## 安全模型

明确一下本工具的信任边界，便于你评估风险：

- **凭据落盘**：快照与登录态中的敏感字段使用 AES-256-GCM（`enc:v1`）加密，
  密钥由本机身份（平台 + home 目录 + 用户名）派生。这意味着：
  - 快照文件被拷贝到**其他机器**后无法解密；
  - 但**同一台机器上的其他进程**（以同一用户身份运行）理论上可以派生出
    相同密钥。这是沿用 ZCode 客户端自身的设计，本工具未改变该边界。
- **多开实例**：启动多开实例时，本工具通过环境变量 `ZCODE_CREDENTIAL_SECRET`
  向实例进程传递解密密钥（实例使用隔离数据目录，密钥不再能从其 home 派生）。
  同机同用户的其他进程可以看到该环境变量——这与上一条「同机同用户即同一
  信任域」的边界一致（沿用 Electron 版多开设计），本工具未改变该边界。
- **网络**：仅访问 Z.ai / BigModel 官方接口（登录、换发令牌、额度查询），
  OAuth 回调只监听 `127.0.0.1` 随机端口且授权完成即关闭，不开放任何入站服务。
- **无遥测**：本工具不收集、不上报任何数据。
- **写入范围**：只写 `~/.zcode/v2/`（登录态）与 `~/.zcas/`（自有数据），
  不触碰其他任何文件。
